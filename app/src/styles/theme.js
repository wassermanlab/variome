import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#4d6397',
        },
        secondary: {
            main: '#ebecec',
        },
        background: {
            default: '#f3f3f3'
        }
    },
});

export default theme;